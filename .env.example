# No environment file or application backend is required.
# The frontend uses public PeerJS signaling and public STUN by default.
# Optional public build-time settings are documented in frontend/.env.example.
# Old DATABASE_URL, LIVEKIT_*, and TURN_* settings are no longer used.
